{
  "name": "ha-homewizard-instant-release-tools",
  "private": true,
  "version": "1.0.2",
  "description": "Release tooling for the HomeWizard Instant Home Assistant integration",
  "scripts": {
    "changeset": "changeset",
    "changeset:version": "changeset version && node scripts/sync-release-version.mjs",
    "changeset:pre:enter": "changeset pre enter rc",
    "changeset:pre:exit": "changeset pre exit",
    "release:tag": "node scripts/create-release-tag.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7"
  }
}